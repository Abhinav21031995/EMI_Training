import { AssignInitialPipe } from './assign-initial.pipe';

describe('AssignInitialPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignInitialPipe();
    expect(pipe).toBeTruthy();
  });
});
